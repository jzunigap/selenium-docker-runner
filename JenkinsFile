pipeline {
    agent any
    stages {
        stage('Run Test') {
            steps {
                //sh
                sh "docker-compose up"
            }
        }
        stage('Bring Grid Down') {
            steps {
                //sh
                sh "docker-compose down"
            }
        }
       
    }
}